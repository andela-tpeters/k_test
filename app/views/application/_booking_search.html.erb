<section id="home-slider">
    <main>
        <div class="container">
            <div class="row">
                <div class="main-slider">
                    <div class="col-md-5 col-sm-12">
                        <% if signed_in? %>
                            <div class="location-div">
                                <h1>Hi, <%= @user.first_name %></h1>
                                <h3 class="location"></h3>
                            </div>
                        <% else %>
                            <div class="slide-text">
                                <h1>Let's Fly, Fly Away</h1>
                                <p>Don't think about the journey... We have it all figured out!</p>
                                <a href="#" class="btn btn-common" data-toggle="modal" data-target="#loginModal">SIGN IN</a>
                            </div>
                        <% end %>
                    </div>
                    <div class="col-md-7 col-sm-12">
                        <%= render 'application/search_form' %>
                    </div>
                </div>
            </div>
        </div>
    </section>
    <div class="cd-overlay"></div>
</main>

<div class="cd-flight view-flights">
    <%= render partial: 'flights/search_results', locals: { flights: @flights, depart_date: Date.today.to_s } %>
</div>
<a href="#0" class="cd-flight-close fa fa-remove"></a>
<%= render partial: 'flights/search', formats: [:js] %>