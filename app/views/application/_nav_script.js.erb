<script>
  $(function(){
    $(document).on('submit', '#avatar_form', function(event){
      event.preventDefault();
      // $.post( $(this).prop('action'), $(this).serialize(), function( data ) {
      //   $(".dropdown.nav-link").html("<%= escape_javascript render 'users/profile_dropdown' %>");
      // });
    });
  });

</script>